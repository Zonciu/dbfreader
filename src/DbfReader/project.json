{
    "buildOptions": {
        "emitEntryPoint": false,
        "preserveCompilationContext": true,
        "debugType": "portable"
    },
    "dependencies": {
    },
    "frameworks": {
        "netcoreapp1.0": {
            "dependencies": {
                "Microsoft.NETCore.App": {
                    "type": "platform",
                    "version": "1.0.0"
                }
            },
            "imports": [
                "dotnet5.6",
                "dnxcore50",
                "portable-net45+win8"
            ]
        }
    },
    "title": "DbfReader",
    "version": "1.3.0",
    "authors": [ "Matt Schwartz" ],
    "description": "Reads and parses DBF files for dotnet core without using the datatable stuff that got ixnayed in core",
    "packOptions": {
        "projectUrl": "https://github.com/mattschwartz/dbfreader",
        "tags": [ "dbf", "dotnet", "core" ],
        "releaseNotes": "Changed name of DbfReader class to DbfReaderUtil so it doesn't conflict with its namespace anymore. Also caching column data so it doesn't have to reparse it every time."
    },
    "supports": {}
}
